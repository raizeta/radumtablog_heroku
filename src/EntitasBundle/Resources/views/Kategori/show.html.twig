{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Kategori</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Namakategori</th>
                <td>{{ entity.namaKategori }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('kategori') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('kategori_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
