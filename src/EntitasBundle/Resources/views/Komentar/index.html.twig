{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Komentar list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Isikomen</th>
                <th>Createat</th>
                <th>Updateat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('blog_komentar_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.isikomen }}</td>
                <td>{% if entity.createAt %}{{ entity.createAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updateAt %}{{ entity.updateAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('blog_komentar_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('blog_komentar_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('blog_komentar_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
